# Salon Telegram Bot (demo)

## Local run
1) Create .env: BOT_TOKEN=..., ADMIN_CHAT_ID=...
2) pip install -r requirements.txt
3) python bot.py

## Deploy to Render (free)
1) Push this repo to GitHub
2) Render → New → Blueprint → select repo
3) Set env vars: BOT_TOKEN, ADMIN_CHAT_ID
